/**
 * Drizzle schema — Phase 0 stub.
 * Day 1: merge Better Auth tables (user, session, account, verification)
 * via `pnpm --filter api exec @better-auth/cli generate`, then define
 * merchants / products / orders below.
 */
export {};
