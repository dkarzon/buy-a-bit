/**
 * Better Auth instance — Day 1.
 * Mount on Hono at /api/auth/* and wire drizzleAdapter + email/password
 * (Pinch OAuth via genericOAuth on Day 2).
 */
export const auth = null as unknown as never;
