/**
 * Better Auth client — Day 1.
 * import { createAuthClient } from "better-auth/react";
 * export const authClient = createAuthClient({ baseURL: import.meta.env.VITE_API_URL });
 */
export const authClient = null as unknown as never;
